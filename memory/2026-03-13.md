# 2026-03-13 记忆日志

## 10:52 - qmd 中文分词问题深度排查

### 核心发现

**分词根因定位：BuiltinMemoryManager**

```js
// manager--Ispw7m5.js 等文件中
async search(query, opts) {
  const cleaned = query.trim();
  const keywords = extractKeywords(cleaned);  // <-- 分词发生在这里！
  const searchTerms = keywords.length > 0 ? keywords : [cleaned];
  ...
}
```

子代理之前修复的是 `qmd-manager` 的 `buildSearchArgs()`，但分词实际发生在 **BuiltinMemoryManager** 中，比 qmd 调用更早。

### OpenClaw Memory Search 架构

```
memory_search tool
    ↓
getMemorySearchManager()
    ↓
FallbackMemoryManager { primary: QmdMemoryManager, fallback: MemoryIndexManager }
    ↓ (qmd 失败时)
MemoryIndexManager.search() → extractKeywords() → 分词 → Gemini embedding
```

关键代码在 `dispatch-BP0viZiL.js`:
```js
const wrapper = new FallbackMemoryManager({
  primary,
  fallbackFactory: async () => {
    const { MemoryIndexManager } = await loadManagerRuntime();
    return await MemoryIndexManager.get(params);
  }
});
```

### 多 dispatch 文件发现

| dispatch 文件 | 加载的 qmd-manager |
|--------------|-------------------|
| dispatch-BP0viZiL.js | qmd-manager-dh7uXqte.js |
| dispatch-Cerq29sy.js | qmd-manager-CtkewJE5.js |
| dispatch-Cndjtt0g.js | qmd-manager-ZTwl54JB.js |
| dispatch-CQsjmw7g.js | qmd-manager-d_bV4Ev1.js |
| dispatch-UogiJYul.js | qmd-manager-DGQDDuGs.js |

所有 qmd-manager 文件已在 09:05 被补丁修复（`const normalizedQuery = query`），但问题在更上游。

### Collection 状态

- 手动 qmd: `memory-dir-main` 存在且正常工作
- OpenClaw 日志: "Collection not found: memory-dir-main"
- XDG 路径一致: `/root/.openclaw/agents/main/qmd/xdg-config`
- 原因未明：可能是运行时环境/状态目录不一致

### 当前结论

1. **qmd 本身正常** - 手动命令可搜索到正确结果
2. **补丁有效但不完整** - `buildSearchArgs` 已修复，但分词在 BuiltinMemoryManager
3. **问题在集成层** - OpenClaw 的 FallbackMemoryManager 或更上游的预处理
4. **需要进一步排查** - 找到 OpenClaw 调用链中哪里先对 query 做了分词

### 网关状态

- 已重启，服务正常 (pid: 222884)
- RPC probe ok
- 但 qmd 集成仍未生效，memory_search 仍 fallback 到 Gemini

### 待解决问题

1. 找到 OpenClaw 在调用 qmd 之前对 query 进行分词的具体位置
2. 解决 collection 状态不一致问题
3. 让 OpenClaw 真正使用 qmd 而非 fallback 到 Gemini

---

## 技术笔记

### 关键文件路径

- OpenClaw dist: `/root/.nvm/versions/node/v22.22.0/lib/node_modules/openclaw/dist/plugin-sdk/`
- BuiltinMemoryManager: `manager-*.js` 文件
- qmd 配置: `/root/.openclaw/agents/main/qmd/xdg-config/qmd/index.yml`
- 网关日志: `/tmp/openclaw/openclaw-2026-03-13.log`

### 替代方案（如 qmd 无法修复）

根据之前调研报告 `/root/.openclaw/workspace/memory/memory-retrieval-solutions.md`:
- 主推: **LightRAG + BGE-M3**
- 备选: **MiniRAG**
